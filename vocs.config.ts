import { defineConfig } from "vocs";

export default defineConfig({
  title: "nodyn",
  sidebar: [
    {
      text: "Foundry",
      items: [
        {
          text: "forge",
          link: "/forge",
        },
        {
          text: "cast",
          link: "/forge",
        },
      ],
    },
  ],
});
